run playbook
  ansible-playbook -i inventory.yml disable_firewall

git repo update
  git add "filename'
  git commit -m "message"
  git push origin main

reset local branch to what's at remote
  git reset --hard origin/main
